# Create class
1. Click on Bundle dir
2. right click -> new -> Form
3. name GenusFormType
4. in BuildFormm create fields:
```
$builder
    ->add('name')
    ->add('speciesCount')
    ->add('funFact');
```