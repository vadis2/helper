# HANDLEBARS. GET STARTED
## install
```
npm install --save handlebars
```
## workflow of creating
Examples:

simple.html

with_helper.html.

## precompiling
Install globally:
```
sudo npm install handlebars -g
handlebars ./components/teamlist/teamlist.tpl.hbs -f
```